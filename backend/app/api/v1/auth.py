"""Deprecated compatibility module.

Authentication routes are implemented and registered from ``app.routes.auth``.
This module intentionally exposes no router to prevent placeholder endpoints
from being registered accidentally.
"""
